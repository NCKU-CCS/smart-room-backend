version: '3'
services:
  smart-room:
    image: smart-room:latest
    build: .
    container_name: SMART_ROOM
    restart: always
    env_file: ./production_env
    ports:
      - 5000:5000

volumes:
  grafana-storage: